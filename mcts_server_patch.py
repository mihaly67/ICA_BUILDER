# This is a snippet to inject into ica_mcp_server.py's deep_planning tool
import json
import ica_mcts_planner

# (We will use a bash sed/awk script or Python string replacement to patch ica_mcp_server.py remotely to return tree data)
